#include <iostream>
#include <math.h>


int main(){
    float a, b, x, z;
    float first = a*pow(sin(x), 2) + b*cos(z*x+a);
    float second = pow((a+b*x), 2) - sin(a+z*x);
    float third = sqrt(x-(sin(b*x+z)));
}